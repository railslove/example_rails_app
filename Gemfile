ruby '2.x.x'

# ...

gem 'pg'
gem 'sentry-raven'
gem 'haml-rails'

# feature toggles
gem 'flipper'
gem 'flipper-ui'
# maybe gem 'flipper-redis'

group :development, :test do
  gem 'dotenv-rails'
end

group :test do
  gem 'rspec-rails'
  gem 'factory_bot'
  gem 'webmock'
  gem 'database_cleaner'
end
